# Анализ неиспользуемых полей в моделях проекта

## Обзор

Данный документ содержит анализ полей в моделях Django-приложений проекта, которые не используются в шаблонах, представлениях или админке.

**Дата последнего обновления**: Декабрь 2024

## Методология анализа

Анализ проводился по следующим критериям:

1. Использование полей в шаблонах (templates)
2. Использование полей в представлениях (views)
3. Использование полей в админке (admin)
4. Использование полей в сериализаторах (если есть)

## Результаты анализа

### 1. Приложение `facilities`

#### Модель `Clinic`

**Статус**: ✅ Актуально

**Поля, которые могут быть неиспользуемыми:**

- `emergency_support` - используется только в админке, не отображается в шаблонах
- `has_hospital` - используется только в админке, не отображается в шаблонах

**Примечание**: Проблема с `clinic.min_price` была решена - поле не используется в шаблоне `clinic_card.html`.

#### Модель `RehabCenter`

**Статус**: ✅ Актуально

**Поля, которые могут быть неиспользуемыми:**

- `capacity` - используется только в детальном шаблоне
- `program_duration` - используется только в детальном шаблоне
- `accommodation_conditions` - используется только в детальном шаблоне

#### Модель `FacilityImage`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `image_type` - не используется в шаблонах
- `title` - не используется в шаблонах
- `description` - не используется в шаблонах
- `is_main` - не используется в шаблонах
- `order` - не используется в шаблонах

#### Модель `FacilityDocument`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `number` - не используется в шаблонах
- `issue_date` - не используется в шаблонах
- `expiry_date` - не используется в шаблонах

### 2. Приложение `staff`

#### Модель `MedicalSpecialist`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `achievements` - не используется в шаблонах
- `schedule` - не используется в шаблонах (только в FacilitySpecialist)

#### Модель `FacilitySpecialist`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `schedule` - не используется в шаблонах

#### Модель `SpecialistDocument`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `number` - не используется в шаблонах
- `issue_date` - не используется в шаблонах
- `expiry_date` - не используется в шаблонах

### 3. Приложение `blog`

#### Модель `BlogPost`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `meta_title` - не используется в шаблонах
- `meta_description` - не используется в шаблонах

#### Модель `BlogImage`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `title` - не используется в шаблонах
- `description` - не используется в шаблонах
- `order` - не используется в шаблонах

#### Модель `Tag`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `icon` - не используется в шаблонах
- `description` - не используется в шаблонах
- `is_system` - используется только в коде, не в шаблонах

### 4. Приложение `requests`

#### Модель `AnonymousRequest`

**Статус**: ✅ Актуально

**Устаревшие поля** (отмечены как deprecated):

- `content_type` - заменено на `organization_type`
- `object_id` - заменено на `assigned_organization`
- `preferred_facility` - заменено на новую систему назначения

**Поля, используемые только в админке:**

- `commission_amount` - используется в админке и печатных отчетах
- `commission_received_date` - используется в админке и печатных отчетах
- `commission_document` - используется в админке

#### Модель `DependentRequest`

**Статус**: ✅ Актуально

**Поля, используемые только в админке и печатных отчетах:**

- `age` - используется в админке и печатных отчетах
- `addiction_duration` - используется в админке и печатных отчетах
- `previous_treatment` - используется в админке и печатных отчетах
- `current_condition` - используется в админке и печатных отчетах
- `preferred_treatment` - используется в админке и печатных отчетах
- `emergency_contact` - используется в админке и печатных отчетах
- `emergency_phone` - используется в админке и печатных отчетах

### 5. Приложение `recovery_stories`

#### Модель `RecoveryStory`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `meta_title` - не используется в шаблонах
- `meta_description` - не используется в шаблонах
- `conclusion_text` - не используется в шаблонах

### 6. Приложение `content`

#### Модель `Banner`

**Статус**: ⚠️ Требует внимания

**Поля, которые могут быть неиспользуемыми:**

- `description` - не используется в шаблонах
- `start_date` - не используется в шаблонах
- `end_date` - не используется в шаблонах

## Рекомендации

### 1. ✅ Решенные проблемы

1. **Проблема с `clinic.min_price`** - решена, поле не используется в шаблоне
2. **Устаревшие поля в модуле заявок** - отмечены как deprecated, заменены новой системой

### 2. 🔄 Приоритетные улучшения

1. **SEO-поля** - добавить использование `meta_title` и `meta_description` в шаблонах
2. **Дополнительная информация** - рассмотреть использование полей `achievements`, `schedule` в шаблонах
3. **Документы** - добавить отображение информации о документах (даты, номера)

### 3. 📊 Анализ использования

1. **Поля только в админке** - большинство полей используются в админке, что нормально
2. **Печатные отчеты** - многие поля используются в печатных отчетах, что оправдывает их наличие
3. **Устаревшие поля** - в модуле заявок устаревшие поля отмечены и не используются в новых разработках

### 4. 🎯 Рекомендации по оптимизации

1. **Добавить документацию** - для полей, которые используются только в админке
2. **Унифицировать логику** - привести к единому стилю отображения информации
3. **Рассмотреть удаление** - полей, которые действительно не используются нигде

## Заключение

**Основные выводы:**

1. ✅ **Критические проблемы решены** - нет блокирующих ошибок
2. ⚠️ **SEO-поля не используются** - рекомендуется добавить их использование
3. ✅ **Модуль заявок актуален** - устаревшие поля отмечены и заменены
4. 📊 **Большинство полей оправданы** - используются в админке или печатных отчетах

**Рекомендуемые действия:**

1. Добавить использование SEO-полей в шаблонах
2. Рассмотреть добавление отображения дополнительной информации
3. Продолжить мониторинг использования полей при развитии функциональности
